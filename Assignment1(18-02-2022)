#Program for print count of n prime numbers.

n=int(input("Enter the number : "))
print("Prime numbers between 1 to n numers : ")
for num in range(0,n+1):
   if num > 1:
       for i in range(2, num):
           if (num % i) == 0:
               break
       else:
           print(num)
